import tensorflow as tf
from tensorflow import keras
from tensorflow.keras import layers


def get_dataset(data_path) -> tf.data.Dataset:
    ds_train, ds_val, ds_test = 0, 0, 0

    return (ds_train, ds_val, ds_test)
